# Google Tag Manager container (contains GA4 G-H2QR3QCZQH).
# Production only: this file is not read by `next dev`, and the runtime
# gate in lib/analytics.ts additionally requires VERCEL_ENV=production,
# so preview deployments do not load it.
NEXT_PUBLIC_GTM_ID=GTM-5ZJ8MZKQ
